<% if @current_user.admin? %>
  <ul class="menu">
    <li><%= link_to "Invoice", invoice_path(@user.id) %></li>
    <li><%= link_to "Credit Note", credit_note_path(@user.id) %></li>
    <li><%= link_to "Payment", payment_path(@user.id) %></li>
  </ul>
<% end %>
